WARNING:

This folder contains leakage-prone features including direct label proxies,
identifier-based variables, and attack-derived telemetry.

These columns should NOT be used for fair intrusion-detection benchmarking
because they may produce artificially inflated performance through shortcut
learning and target leakage.

For reproducible leakage-safe evaluation, use the dataset provided in the
data/leakage_safe/ directory.
